# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index

GET     /authorized                 controllers.Application.authenticated

GET     /facebookConnected          controllers.Application.connected

POST    /complete                   controllers.Application.complete

#Facebook hooks
GET     /webhook                    controllers.Application.webhook

POST    /webhook                    controllers.Application.webhookPost

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

# Social Login

GET         /authenticate/:provider          controllers.SocialAuthController.authenticate(provider)

GET         /connect/:provider               controllers.Application.connect(provider)

    